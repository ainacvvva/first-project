# ⤷ Project Title: Мій перший адаптивний макет на основі дизайну з Behance”

## ↬ Features:
<h3 align="center">»»————-　★　————-««</h3>
<h3 align="center">◌ Адаптивна верстка з використанням медіа-запитів ◌</h3>
➥ Для коректної адаптації я використовувала два основних медіа-запити:<br>

• `@media (max-width: 1290px) and (min-width: 375px)`❞ - адаптувала під неповний екран браузера та під мобільні пристрої.

• `@media (max-width: 790px)`❞ - дозволив адаптувати при найвужчому екрані браузера.

<h3 align="center">◌ Бургер-меню, яке з’являється на менших екранах ◌<br></h3>
• Бургер-меню приховане на великих екранах і з’являється лише при зменшенні ширини вікна або на мобільних пристроях.

<h4>➥ На повному екрані:<br> </h4>

```css
.dropdown-button {
  display: none; 
}
```

<h4>➥ На планшетах і мобільних пристроях:<br> </h4>

```css
@media (max-width: 1290px) and (min-width: 375px) {
  .dropdown-button {
    display: block;
  }
}

```
<h4 align="center">- JS-логіка роботи меню: </h4>

<h4>➥Спочатку я підключаю кнопки і список меню через `DOM`:</h4>

```css
const menuButton = document.querySelector('.fa-bars'); 
const menu = document.querySelector('.dropdown-list'); 
```

<h4>➥Потім призначаю подію кліку щоб потім надасть умову:</h4>

```css
menuButton.onclick = () => { ... }
```

<h4>➥Усередині функції даю умову меню:<br></h4>

• Якщо меню буде строго дорівнювати `'flex' `, то при кліку на кнопку menuButton блок з елементами зникне. Якщо не натискати, то блок з меню буде видно.
```css
if (menu.style.display === "flex") {
  menu.style.display = "none";
} else {
  menu.style.display = "flex";
}
```

<h3 align="center">◌ Slider ◌</h3>

<h4>➥ Підключення елементів через `DOM` </h4>

```js
const slides1 = document.querySelector('.slides1');
const countSlide = document.querySelectorAll('.slide1').length;
let index = 0;
```
• `querySelectorAll('.slide1')` — отримує всі елементи з класом .slide1.

• `.length` — повертає кількість елементів, знайдених методом `querySelectorAll`

• `index = 0` — початковий слайд.


<h4>➥Логіка кнопок «вперед». Кнопка наступного слайда:<br> </h4>

```js
document.getElementById('next').onclick = () => {
  if (index < countSlide - 1) index++;
  updateCountSlide();
}
```

• `getElementById('next')` — підключення кнопки за id.

• `.onclick` — подія кліку.

• `if (index < countSlide - 1)`
– умова, в якій є цикл. Якщо, наприклад, слайд 2 (index1) < загальна кількість слайдів, то перегортання праворуч відбудеться. 

• `index` — наші слайди, 

• `countSlide` — загальна кількість слайдів

• `index++` — збільшує індекс, тобто перемикає слайд вправо.


<h4>➥Логіка кнопок «назад»</h4>

```js
document.getElementById('prev').onclick = () => {
  if (index > 0) index--;
  updateCountSlide();
}
```
• `(index > 0)` — слайди, які більші за index0, — перегортає ліворуч. 

• `index--` — виконує перегортання ліворуч.


<h4>➥ Функція перемикання слайдів</h4>

```js
function updateCountSlide() {
  const slideWidth = document.querySelector('.slide1').offsetWidth;
  slides1.style.transform = `translateX(${-index * slideWidth}px)`;
}
```
• `offsetWidth` — повертає поточну ширину слайда і автоматично підлаштовується під адаптив.

• `transform: translateX(...)` — рухає блок зі слайдами по осі X.

• `-index * slideWidth` — пересуває на право и помножує на довжину слайдов


<h4 align="center">- Доповнення</h4>

<h4> ➥Коли я перевіряла адаптацію слайдерів і перегорнула — було чудово, але коли я збільшила розмір екрана — слайди почали плавно самі рухатися з місця. Щоб це виправити, я додала цей код:</h4>

```js
window.addEventListener('resize', () => {
  slides1.style.transition = 'none';
  updateCountSlide();

  setTimeout(() => {
    slides1.style.transition = '';
  }, 50);
});
```

• ` window.addEventListener('resize' ` — реагує на зміну розміру вікна браузера
• `slides1.style.transition = 'none'; ` — вимикаю роботу transition, яка в CSS відповідає за плавність прокрутки:

```css
.slides1 {
  display: flex;
  transition: transform 0.5s ease;
}
```

➥ Це було зроблено для того, щоб під час зміни розміру вікна браузера слайди не смикалися.

• ``updateCountSlide()`` — виконання функції

• ``setTimeout(..., 50)`` — через 50 мілісекунд анімація плавності прокрутки повертається назад.


<h3 align="center"> ◌ Smooth CSS(SCSS) animations ◌<br> </h3>

➥ У проєкті я використовувала плавні CSS-анімації як для зміни стилів, так і для переміщення елементів.
Основою слугує властивість `transition: .5s;`, завдяки якій елементи змінюють свій стан плавно — наприклад, при наведенні курсора. Використовуючи псевдоклас `&:hover`, я можу змінювати зовнішній вигляд або положення елемента без різких стрибків. Окрім цього, для складніших ефектів я застосувала `@keyframes`. За їх допомогою я створила два різні типи анімацій:

• Перший заголовок — з’являється, а потім зникає.

• Назва вулиці та інший текст — з’являється один раз і залишається на місці.


<h4>➥ Заголовок зникає після появи <h4>

```css
.factorium-main-title {
  animation: fadeOutThenUp 2s ease forwards;
}
```

• `fadeOutThenUp` — ключова анімація, яка спочатку робить елемент видимим, а потім поступово піднімає і знову приховує його.

• `2s` — тривалість анімації.

• `ease` — тип кривої переходу (анімований елемент починає рух повільно, прискорюється в середині і знову сповільнюється наприкінці).

• `forwards` — зберігає кінцевий стан анімації, щоб елемент не повертався до початкового вигляду.

<h4>➥ А щоб додати ефект потрібно:<h4>

```css
@keyframes fadeOutThenUp {
  0% {
    opacity: 0;
    transform: translateY(15px);
  }
  50% {
    opacity: 1;
    transform: translateY(0);
  }
  100% {
    opacity: 0;
    transform: translateY(-15px);
  }
}
```

• `0%` — елемент нижче своєї позиції та невидимий

• `50%`— повністю видно, знаходиться у вихідній точці

• `100%` — піднявся на 15px і знову став прозорим


<h4>➥ Заголовок та вулиця з’являються і залишаються</h4>

```css
.center-home-kiev,
.street {
  animation: fadeInThenStay 2s 2s ease forwards;
}
```

• `fadeInThenStay` - з'являється та залишається 

• перша `2s` → тривалість анімації

• друга `2s` → затримка перед запуском

<h4>➥ Додати ефект потрібно:<h4>

```css
@keyframes fadeInThenStay {
  0% {
    opacity: 0;
    transform: translateY(15px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}
```
• `0%` - нижче своєї позиції та невидимий

• `100%` - з’являються плавно і залишаються на своїх позиціях



## ↬ Technologies:
◌ HTML, CSS (SCSS), JavaScript

## ↬ Design:
Design by [Kate Lavrova💙💛](https://www.behance.net/katelavrova) <br> 
Code by Ainacvvaa<br>
<h2 align="center">‎ ♡‧₊˚✧ ૮ ˶ᵔ ᵕ ᵔ˶ ა ✧˚₊‧♡ </h2>

